# TODO

## 🔴 멀티플레이: 동료 PC 에서 서로를 못 찾음 (보류 — 2026-09-08)

**증상**: 초대 링크(`?room=XXXX&h=...`)를 동료에게 보내 들어와도 **모두 각자 "1/4 · 내가 방장"** 상태. 서로의 존재를 발견하지 못함.

**지금까지 확인된 사실** (2026-09-08, 유저 PC = 회사망):

| 확인 | 결과 | 의미 |
|---|---|---|
| Nostr 기본 릴레이 28개 WSS 연결 (이 PC) | **26/28 성공** | 이 PC 에서 시그널링 인프라 자체는 정상 |
| 같은 PC, **별도 브라우저 컨텍스트** 2개가 라이브 Pages 에서 만나는가 | **2초 만에 성공** | 코드의 방 참가·명단 로직은 정상. 같은 머신 안에서는 됨 |
| 같은 PC 두 탭 (Trystero 실 P2P) | 성공, 대전까지 정상 | 메시지 교환·라운드 동기 정상 |

→ **"코드가 방을 못 만드는" 문제가 아니라, 서로 다른 머신 사이의 연결 단계에서 막히는 것**으로 좁혀짐. 같은 머신 테스트는 실제 NAT 통과를 전혀 시험하지 않는다.

**유력 원인 (순위)**

1. **WebRTC 피어 연결 실패 (NAT/방화벽, TURN 없음)** — Trystero 의 `onPeerJoin` 은 시그널링이 아니라 **데이터 채널이 열린 뒤** 발화한다. 시그널링은 성공했어도 ICE 가 실패하면 상대가 영원히 "안 보임". 지금 `rtcConfig` 에 STUN 만(기본) 있고 **TURN 서버가 없다**. 회사망(대칭 NAT·클라이언트 격리)에서 가장 흔한 실패.
2. 동료 쪽 네트워크가 Nostr 릴레이(wss)를 차단 — 이 PC 는 통과했지만 동료 PC/집 네트워크는 다를 수 있음.
3. 동료가 옛 빌드를 캐시로 보고 있음 — 우하단 `build <시각>` 으로 확인 가능.

**다음에 할 일** (예상 반나절)

- [ ] `net.js` Trystero `joinRoom` 에 **TURN 서버 추가** (`rtcConfig: { iceServers: [...] }`). 무료 공개 TURN 예: Open Relay(`turn:openrelay.metered.ca:80`, user/cred `openrelayproject`) — 우선 이것만으로 1번이 해결되는지 확인
- [ ] **대기실 진단 표시**: 연결된 릴레이 수(`getRelaySockets()`), 시그널링에서 본 피어 수 vs 실제 연결된 피어 수, ICE 상태, 내 `selfId` 앞 6자리. "안 됨"을 "어디서 막힘"으로 바꿔 준다
- [ ] **전송 전략 폴백**: `?net=mqtt` / `torrent` (Trystero 는 `trystero/mqtt`, `trystero/torrent` 도 제공). 릴레이 연결 0 이 5초 지속되면 자동 안내
- [ ] 위 세 가지 넣고 **동료 PC 와 실제로 재현**하며 진단 표시로 어느 단계인지 확정
- [ ] 그래도 안 되면 작은 중계 서버(PartyKit / Cloudflare Worker + Durable Object, 수십 줄)로 전환 — Pages 밖 조각이 하나 생기지만 연결은 확실

**동료와 재현할 때 콘솔에서 볼 것** (F12 → Console):
```js
__rd.roster()          // 명단 길이 — 1 이면 아직 아무도 못 찾음
__rd.selfId()          // 내 id
__rd.isHost()
```
양쪽 모두 `roster()` 가 1 이면 시그널링 또는 ICE 단계 실패. 한쪽만 2 면 비대칭 NAT.

---

## 그 외

- [ ] 사운드 (WebAudio 합성, 에셋 0): 레이저 발사·피격·벽 튕김·쿨다운 준비
- [ ] 아이템: "이미 우주복을 입은 상태에서 아머" 처리 — 현재 무효. 아깝다고 느껴지면 배리어 1겹 등으로
- [ ] 봇이 아이템을 노리고 움직이지 않음 (지나가다 우연히 먹기만 함) — 아이템 쪽으로 반동 이동하는 판단 추가 여지
