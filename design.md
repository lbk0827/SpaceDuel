# Recoil Duel — 반동 하나로 싸우는 우주인 레이저 결투 (2D) 설계

- 작성일: 2026-09-08
- 상태: 설계 승인(대화) → 구현 진행
- 위치: `D:\Vibe_Coding\_shelf\recoil-duel\` — 이 문서·계획·코드 전부. 자매: `../bubble-bash-fiesta/`(같은 컨벤션: 바닐라 ESM + CDN importmap + node:test + 튠 패널 + esbuild standalone).

## 1. 한 줄 정의

> 우주인 둘(플레이어 vs AI)이 닫힌 방에서 레이저 총으로 결투한다. **입력은 클릭 하나** — 클릭하면 총구가 지금 향한 방향으로 레이저가 나가고, 몸은 그 **반동으로 반대로 밀리며 회전**한다. 이동·조준 버튼이 없다. 반동이 유일한 이동·조준 수단이며, 레이저에 맞으면 즉사.

재미의 핵심: 몸이 돌아가는 걸 지켜보다가 **총구가 원하는 곳을 향하는 순간** 클릭하는 타이밍, 그리고 그 결과(반대로 밀림 + 회전)가 물리적으로 일관되어 예측·학습이 되는 것.

## 2. 확정된 결정 (2026-09-08 대화)

| 항목 | 결정 | 기각 |
|---|---|---|
| 차원·스택 | 2D, Canvas 2D 렌더 + **Rapier2D**(compat, WASM 인라인, CDN importmap) | Matter.js(가능하나 고정 스텝·충격 처리는 Rapier 가 안정적, 자매 프로젝트와 같은 계열) |
| 입력 | **클릭 = 발사 하나.** 마우스 위치는 무관. 총은 몸에 고정되어 앞을 향함 | 마우스 방향 조준(초안이었으나 정정됨) |
| 반동 | **물리 그대로** — 총구 위치에 총구 반대 방향 임펄스. 밀림·회전은 전부 엔진 계산. 총이 몸 중심보다 위(어깨)에 있어 항상 같은 쪽으로 돈다 = 예측 가능한 규칙 | 매 발 회전 방향 교대, 자세 따라 방향 결정, 자립 토크(기본 0, 슬라이더로만) |
| 피격 | **한 방 즉사**, 라운드제(기본 3선승) | 체력제, 밀어내기 아웃 |
| 경기장 | **닫힌 방**(바닥·벽·천장) | 천장 없음, 낭떠러지 |
| 발사 제약 | **쿨다운 / 에너지 두 모드 모두 구현**, 튠 패널 토글로 전환. 값 전부 슬라이더 | 재장전 리듬 |
| 레이저 | 눈에 보이는 속도의 탄(히트스캔 아님). 플레이어 **파랑**, AI **빨강** | — |
| 슬로모션 | 탄이 상대에게 맞을 것으로 예측되는 구간에서 시간 느려짐 | — |
| AI | **명중 예측 사격**(지금 쏘면 표적 속도로 예측한 몸에 실제로 닿는가를 선분-캡슐 스윕으로 판정) + **회피 사격**(내게 맞을 탄이면 반동 후 위치를 예측해 피해질 때 즉시 발사) + **똑똑한 재배치**(반동 회전이 총구를 표적 쪽으로 돌릴 때만). 허용각 3°·반응 0.05s·재배치 1.2s·회피 예측 0.45s 슬라이더 = 난이도. (2026-09-08 유저 요청 "더 똑똑하고 어렵게"로 강화) | 더미 |
| 스폰 | 라운드 시작 시 화면 위에서 낙하. 플레이어 좌, AI 우, **Y 를 다르게**(플레이어 낮게·AI 높게, 라운드마다 교대) | — |
| 캐릭터 그림 | **SVG 벡터 우주인**(헬멧·바이저·수트·산소통·총) 파랑/빨강 2종, 코드에 인라인. 몸 회전을 그대로 따라감 | 비트맵 아트(제작 불가), 캡슐 도형만 |

## 3. 게임 형태

| 항목 | 값 |
|---|---|
| 화면 | 가로 16:9 컨테이너(뷰포트에 맞춰 스케일). 월드 16 × 9 m, 1m = 화면폭/16 px |
| 중력 | **−1 m/s²** (슬라이더 −30~0). −12 → −6 → −1 로 낮춤(유저 결정, 거의 무중력) |
| 캐릭터 | **기본 = 다리 없는 둥근 몸(원, 반지름 0.45)** — 착지해도 서지 않고 굴러 임의 방향을 본다(유저 피드백: 캡슐+다리는 착지하면 항상 서 버림). 패널 `bodyShape` 로 캡슐(폭 0.5·높이 1.2)로 되돌릴 수 있음. 질량 1. 각감쇠 0.8 / 선감쇠 0.1 / 마찰 0.6 / **반발 0.6**(벽·바닥에서 튕김, 유저 요청. 결합 규칙 Max) (전부 슬라이더, 즉시 반영) |
| 총 | 몸 로컬 좌표 (앞 +0.45, 위 +0.30). "위 +0.30" = **지렛대**(슬라이더 0~0.6). 총구 방향 = 몸의 +x 축(우주인이 바라보는 쪽) |
| 발사 | 총구에서 레이저 탄 생성, 속도 25 m/s(슬라이더 10~60). 동시에 총구 위치에 임펄스 `recoilImpulse`(기본 6 N·s, 슬라이더 1~15)를 총구 반대 방향으로 |
| 탄 | 길이 1.0 m 광선 + 0.15s 잔상. 벽에 닿으면 소멸. 자기 몸 무시 |
| 즉사 | 탄이 상대 캡슐과 교차하면 라운드 종료 |
| 라운드 | 3선승(슬라이더 1~5). 시작 발사 잠금 없음(spawnLock 0, 슬라이더로 켤 수 있음). 승자 배너 1.5s → 다음 라운드. 매치 종료 → "다시" |
| 발사 제약 | `fireMode`: `cooldown`(기본 0.5s, 슬라이더 0.1~2.0) / `energy`(최대 3발, 1.2s 당 1발 충전, 둘 다 슬라이더). 둘 다 HUD 표시(쿨 = 링, 에너지 = 칸) |
| 슬로모션 | 매 프레임 탄마다: 현재 위치에서 진행 방향으로 `속도 × lookahead(0.25s)` 만큼 선분을 그어 **상대 캡슐(현재 자세)** 과 교차하면 `timeScale = slowmoScale(0.25)`. 교차하지 않으면 1.0 으로 복귀. 화면 가장자리를 살짝 어둡게(비네트) — 이것 하나만 |
| 스폰 | 라운드 r: 플레이어 (−5, 2 + (r%2)·1.5), AI (+5, 3.5 − (r%2)·1.5). 즉 한쪽이 높으면 다른 쪽이 낮다. 초기 회전 0(서 있음), 서로를 마주봄 |

### 규약 (리뷰 반영)

- **좌표**: 원점 = 방 중심, x ∈ [−8, 8], y ∈ [−4.5, 4.5], **y 위쪽**. Canvas 는 y 아래이므로 renderer 가 한 번 flip 한다. 탄용 `arena = { halfW: 8 − 벽두께/2, halfH: 4.5 − 벽두께/2 }` 는 벽 바디의 안쪽 면과 일치.
- **facing**: 각 몸에 `facing ∈ {+1, −1}` 속성. 총 로컬 오프셋 = `(facing × gunOffsetX, gunOffsetY)`, 총구 방향 = `rotate((facing, 0), angle)`, 스프라이트 flip 도 같은 값. 플레이어 +1, AI −1. 초기 각 0 에서 서로를 마주보고, 반동 회전은 둘 다 "총구 반대쪽으로 밀리며 어깨 지렛대만큼 돈다"로 동일 규칙.
- **캡슐**: Rapier `capsule(halfHeight 0.35, radius 0.25)` = 전체 높이 1.2·폭 0.5. `geom` 도 같은 값.
- **시간**: 프레임 델타 × `timeScale` 을 누산기에 넣고 **고정 1/120 스텝**을 반복(Rapier dt 는 항상 1/120). 물리·탄 스윕·firegate·AI 타이머·spawnLock 은 전부 이 **게임 시간**(슬로모 중 쿨다운도 느려짐 — 의도). 라운드 배너 1.5s 만 실시간.
- **firegate 인터페이스**: `createFireGate(tuning)` → `{ tryFire(): bool, update(dt), reset(), state() }`. tuning 을 참조로 들고 있어 모드·값 변경은 다음 판정부터 반영, 상태(쿨 잔여·에너지)는 리셋하지 않음. 라운드 시작 `reset()` = 즉시 발사 가능·에너지 만충.
- **AI 상태**: `decide(self, target, ctx, mem)` — `mem = { onTargetSince, lastGoodAt }` 은 호출자가 보관하고 함수가 변이. 반응 지연은 허용각 **진입 순간**부터 세고 벗어나면 리셋. 리드 사격 비행시간은 2회 반복 근사.
- **명중 판정**: 탄 **선두점**의 from→to 스윕만. 1.0m 광선은 시각 전용.
- **동시 명중**(같은 스텝에 양쪽 탄 모두 교차): 무효 라운드 — 점수 없이 재경기.
- **라운드 종료**: 즉시 timeScale 1, 물리·탄 정지(배너 동안 스텝 안 함), 리스폰 시 탄·잔상 전부 제거.

## 4. 한 판의 처리

1. **입력** — `pointerdown` 아무 곳 → `firegate.tryFire()` 가 허용하면 발사.
2. **발사** — 총구 월드 위치·방향 계산(몸 위치·각) → 탄 스폰 → `body.applyImpulseAtPoint(−dir × recoilImpulse, muzzlePos)`.
3. **탄 진행(순수)** — 고정 스텝(1/120, 슬로모 시 timeScale 곱) 마다 `from→to` 선분을 상대 캡슐(위치·각)과 **선분-캡슐 스윕**, 벽과는 AABB 클램프. 명중 → 라운드 종료 이벤트. 벽 → 소멸.
4. **슬로모 판정(순수)** — `willHit(bolt, targetPose, lookahead)`. 하나라도 true 면 timeScale 낮춤.
5. **AI(순수 결정)** — 입력: 자기 자세·속도, 플레이어 자세·속도, 탄속, 발사 가능 여부, 마지막 좋은 각 시각. 출력: `fire: bool`. 규칙: 플레이어 위치를 비행시간만큼 리드 → 자기 총구 각과 목표각 오차 ≤ `aiTolerance`(6°) 이고 반응지연(0.15s) 경과 → 발사. 좋은 각이 `aiRepositionAfter`(2.0s) 동안 없으면 발사 가능 시 그냥 발사(재배치). 라운드 시작 잠금은 동일 적용.
6. **라운드 종료** — 즉시 timeScale 1, 승자 점수 +1, 배너 → 3선승 확인 → 리스폰.

## 5. 아키텍처

원칙: 순수 로직(`laser`, `firegate`, `ai`)은 Rapier·Canvas 를 모르며 `node:test` 로 검증. 물리는 Rapier2D 가 캐릭터·벽만 담당하고, **탄은 엔진 밖**(순수 스윕 — 결정적이고 터널링 없음).

| 모듈 | 역할 | 의존 |
|---|---|---|
| `src/tuning.js` | 파라미터 단일 출처 + 슬라이더 메타 + 월드 상수 | — |
| `src/geom.js` | 순수 기하: 회전, 캡슐-선분 최근접 거리, 선분 스윕 | — |
| `src/laser.js` | 순수: `stepBolt(bolt, dt, targets, arena) → {bolt, hit?}`, `willHit(bolt, target, lookahead)` | `geom` |
| `src/firegate.js` | 순수 상태기계: `createFireGate(tuning)` → `{ tryFire(), update(dt), reset(), state() }` cooldown/energy | — |
| `src/ai.js` | 순수: `muzzleOf`, `aimError`, `decide(self, target, ctx, mem) → { fire, reason, error }` | `geom` |
| `src/physics.js` | Rapier2D 월드·벽·캡슐 바디, `applyRecoil`, 자세 조회, 고정 스텝 | rapier |
| `src/sprites.js` | 우주인 SVG 문자열(색 파라미터) → `Image` (data URI) | — |
| `src/renderer.js` | Canvas 2D: 배경·벽·캐릭터(SVG 회전)·탄·잔상·슬로모 비네트 | `sprites` |
| `src/hud.js` | 점수·쿨/에너지·라운드 배너 | — |
| `src/panel.js` | 튠 패널 | `tuning` |
| `src/game.js` | 엔트리·루프·라운드·스폰·승패 | 전부 |

## 6. 파라미터(튠 패널)

`gravity −12` · `recoilImpulse 6` · `gunOffsetY 0.30`(지렛대) · `gunOffsetX 0.45` · `angularDamping 0.8` · `linearDamping 0.1` · `friction 0.6` · `restitution 0.1` · `uprightTorque 0`(자립, 기본 끔) · `laserSpeed 25` · `fireMode cooldown|energy` · `cooldown 0.5` · `energyMax 3` · `energyRegen 1.2`(초/발) · `slowmoScale 0.25` · `slowmoLookahead 0.25` · `aiToleranceDeg 6` · `aiReaction 0.15` · `aiRepositionAfter 2.0` · `roundsToWin 3` · `spawnLock 1.0`

## 7. 테스트

- `geom`: 회전 변환, 점-선분 거리, 선분-캡슐 교차(정면/스침/빗나감/이미 겹침)
- `laser`: 한 스텝 진행, 명중 반환, 벽 소멸, `willHit` 참/거짓, 자기 몸 무시
- `firegate`: cooldown 모드(연타 거부·시간 경과 허용), energy 모드(3발 후 거부·충전 후 허용), 모드 전환
- `ai`: 정지 표적 정면 → fire, 오차 큼 → 안 쏨, 리드(움직이는 표적) 각 계산, 재배치 사격 타이머

렌더·물리·연출은 눈으로.

## 8. 범위 밖

사운드, 비트맵 아트, 맵 장애물, 아이템, 온라인, 회피 AI, 모바일 최적화(포인터 이벤트만 지원).

## 9. 산출물

`dist/index.html` standalone(더블클릭 실행) + 개발용 `index.html`(정적 서버) + `node --test test/*.test.js` + README.
